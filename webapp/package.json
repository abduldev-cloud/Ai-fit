{
  "name": "fitmind-ai-web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.7.0",
    "framer-motion": "^11.0.0",
    "lucide-react": "^0.390.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1",
    "react-scripts": "5.0.1",
    "recharts": "^2.12.7",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "node -e \"const fs=require('fs'); if(fs.existsSync('../backend/.env')) fs.copyFileSync('../backend/.env', '.env')\" && react-scripts start",
    "dev": "node -e \"const fs=require('fs'); if(fs.existsSync('../backend/.env')) fs.copyFileSync('../backend/.env', '.env')\" && react-scripts start",
    "build": "node -e \"const fs=require('fs'); if(fs.existsSync('../backend/.env')) fs.copyFileSync('../backend/.env', '.env')\" && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}